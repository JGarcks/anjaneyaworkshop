# Workshop — Progress Log, September 2026

*One entry per session, newest last. Format in `../../../CLAUDE.md` §Progress Log Discipline. Active-Work holds the pointer; this file holds the detail.*

### Session W0 — Planning (September 24, 2026)

Planning only, in Cowork on the laptop, with PC Claude on Garcks-PC answering through `Desktop/for-laptop-claude.txt`. Found the domains in Gmail (Porkbun, 22 Apr 2026, both the `.co.uk` and the `.com`); read Planet's quartet and `server.rs` from a deploy-key clone; learned how Planet runs from PC Claude's note; agreed the shape of the site with Jamie in chat; wrote `WEBSITE_PLAN.md`; then split it into this quartet on Jamie's word, porting Explore's rules.

**What changed.**  `CLAUDE.md` (15.6 KB, over budget, queued), `docs/decisions.md` (WS · D1–D12, P0-a, P0-b), `docs/process/Workshop-Strategic-Plan.md` (19.8 KB), `Workshop-Coverage-Plan.md` (12.4 KB), `Workshop-Active-Work.md` (3.6 KB), `rules-lineage.md`, `secrets-map.md`, `.gitignore`, this file. `WEBSITE_PLAN.md` moved to `docs/archive/`. No code; no git repo yet.

**Leave alone (already correct).** Planet: PC Claude made it a systemd user service on a fixed build of `sc-option1` at 6184a69 during this session; nothing for the Workshop to do there. The laptop's deploy key on `planet` (read-only) and the same key in Garcks-PC's `authorized_keys` (unused: the sandbox cannot reach the LAN).

**Verification.** Not run, no code. Facts checked: Cowork's sandbox refuses LAN addresses (`Forbidden` from the egress proxy on 192.168.1.157, the Tailscale address and the hostname); GitHub over SSH works from it; Planet's viewer uses relative URLs and self-spins (`viewer.js`); its background is `#0b0e14` (`index.html`); "Block by Block" is Mojang's programme with UN-Habitat (blockbyblock.org, Minecraft Wiki).

**Public check.** None yet.

**The walk.** None this session. Jamie's answers in chat: the live planet, experiments included, as the public view; a dark screen with the planet and subtle links; the block graphic for BlockByBlock's room, not the hub; BlockByBlock may need a new name; Anjaneya's origin need not be told on the site.

**Decisions.** WS · D2, D3, D6, D9, D10, D11, D12 — Jamie's, in chat. WS · D1, D4, D8 — Claude's proposals, awaiting the first decision round. WS · D5 open (the name). WS · D7 deferred to Phase 4. P0-a, P0-b open.

**Deferred.** The §8 amendment request into Planet's queue (a Planet session, at Jamie's word, once D1 is agreed). The trim of CLAUDE.md toward 12 KB.

### Session W1 — Phase 0, Ground (September 24, 2026)

The first build session, in the Code tab on the laptop. Opened with the decision round (WS · D1, D4, D8, P0-b); then git, the site scaffold and its first test, the holding page, the Phase 0 public check, and both domains moved to Cloudflare with Pages building from the repo. Jamie did the account steps by hand (GitHub repo and deploy key, Cloudflare sign-up, both domains, Porkbun nameservers); laptop Claude did the Cloudflare configuration through Jamie's Chrome, each click on a list Jamie approved in chat.

**What changed.**  Git: `main` on `github.com/JGarcks/anjaneyaworkshop` (private), pushed over a second deploy key (`id_ed25519_workshop`, W1-b) set as the repo's `core.sshCommand`; GitHub's host key added to the laptop's `known_hosts` after checking its fingerprint against GitHub's published one. `site/`: `package.json` (Vitest 5.0.1, Node ≥ 22.12), `.node-version` (24), `public/index.html` + `style.css` (the holding page: `#0b0e14`, the name centred), `public/_headers` (CSP `default-src 'none'`, nosniff, no referrer, no framing), `public/js/tick.js` (the restart rule), `test/tick.test.js`. `scripts/check-public.sh` (Phase 0: six assertions). `.gitattributes` (LF everywhere), `.claude/launch.json` (serves `site/public` on 5173). Cloudflare: both zones on the Free plan (`.co.uk` → margot/yahir, `.com` → howard/magnolia); Pages project `anjaneyaworkshop` (root `site`, build `npm test`, output `public`, branch `main`) with `anjaneyaworkshop.co.uk` attached; `.co.uk`: proxied `www` → 192.0.2.1 and redirect rule "www to the bare address (W1)"; `.com`: proxied `@` and `www` → 192.0.2.1, the three Porkbun email-forwarding records kept (MX ×2, SPF), redirect rule "The .com to the .co.uk (W1)" (all requests, path and query kept, 301); Always Use HTTPS on both. Porkbun: both domains' nameservers to Cloudflare; the `.com`'s old URL forward to `l.ink` deleted. Docs: decisions table, Strategic Plan status and open questions, secrets map, this entry, Active-Work, Coverage Plan.

**Leave alone (already correct).** Planet — including its restart from year zero on the experimental "T10" rule at 15:07 (PC Claude's note on the Desktop); the site follows (WS · D2) and has no still yet to refresh, so the held trigger needs no action this phase. HSTS is off on both zones: not on this session's approved list, and a year-long browser promise is a decision of its own. The `.com`'s email-forwarding records: harmless to the web, and they keep any `@anjaneyaworkshop.com` forwarding working.

**Verification.** `npm test` in `site/`: 1 passed (1), locally and in Cloudflare's build (Node 24.13.1; the deploy log shows `npm test` → 1 passed before "Success: Your site was deployed!"). `nginx -t`: not run, no change under `frontdoor/`. Link check: not run, no links yet. Mojang scan: not run, no room. Secret scan of every staged diff: clean. Holding page measured at 375×812 and 390×844 in the browser pane: the name centred to the pixel (57/57 and 64/64 either side), no horizontal scroll.

**Public check.** `scripts/check-public.sh`, 15:03 UTC and again 15:10 UTC after the certificate switchover: **6/6 PASS** — `https://anjaneyaworkshop.co.uk/` 200 in 0.18 s; `http://` → 301 https; `www.…co.uk/some/path?x=1` → 301 bare address with path and query; `.com/`, `.com/some/path?x=1`, `www.…com/` → 301 to the `.co.uk`, path and query kept. Budget assertions start in Phase 1.

**The walk.** First try on the laptop (about 15:08 UTC): Chrome showed "This site can't be reached — ERR_QUIC_PROTOCOL_ERROR". Cause, checked from here: Pages was moving the bare address onto its own certificate after the custom domain was attached, and for a few minutes the bare address answered the TLS handshake with alert 40 (no certificate) while `www` and `pages.dev` were fine; the 15:03 public check had passed before the switchover. At 15:11 the new certificate (Google Trust Services WE1) was live, Pages showed Active / SSL enabled, and the public check was 6/6 again. Second try: **works on the laptop and on the phone on cellular** — Jamie. No changes asked for. Lesson: one passing check is a moment, not a state — after attaching a domain, re-run `check-public.sh` once Cloudflare reports it Active (now in the Coverage Plan, Phase 3).

**Decisions.** WS · D1 front door and the §8 request — Jamie, agreed. WS · D4 one-second cache — Jamie, agreed. WS · D8 Cloudflare free plan — Jamie, agreed. P0-a moot (Code tab). P0-b private for now — Jamie. W1-a Pages watches the repo, no token — Jamie. W1-b a second deploy key — Claude, forced by GitHub's one-repo-per-deploy-key rule. Cloudflare signed in with Google as jamie.garcka@gmail.com — Jamie, on Claude's recommendation (main inbox, not the Anthropic-linked address, not GitHub).

**Deferred.** The §8 amendment request text is in the W1 chat for Jamie to hand to a Planet session (rule 8). No phone screenshot in `docs/screens/phase-0/`: headless Edge cannot render below ~500 px wide and cropped the page, and the walk confirmed the phone by eye; Phase 2's set should include one from a real phone. The laptop shot (1440×900) is saved. The AI-crawler setting on the `.co.uk`, read from the live `robots.txt` because Jamie didn't remember it: Cloudflare-managed, `search=yes, ai-train=no`, training crawlers disallowed (W1-c). The §8 request is also appended to `Desktop/for-laptop-claude.txt` for the Planet session to read directly. The CLAUDE.md trim (not touched this session). A `robots.txt` of the site's own is not needed while Cloudflare's managed one carries the crawler settings.

### Session W2 — Phase 1, the front door (September 24, 2026)

Code tab on the laptop. Decision round (W2-a…e, with W2-c explained a second time at Jamie's ask), then the door's config, the tunnel's config, the install script and runbook for PC Claude, the Phase 1 public check, and the Cloudflare Cache Rule. Paused at the seam where PC Claude runs the runbook on Garcks-PC; the phase closes after its report, the public check and Jamie's walk.

**What changed.**  `frontdoor/nginx.conf` (127.0.0.1:8090 → 127.0.0.1:8080; GET/HEAD only; gzip; one-second cache keyed on the path with miss-collapsing; `Cache-Control` by status and path — grid a day, 200/404 one second, errors never kept; `X-Planet-Tick` through; CORS and `frame-ancestors` for the `.co.uk`; 20 r/s per visitor, burst 40, from `CF-Connecting-IP`; an allowlist of Planet's addresses; an access log with no visitor addresses). `frontdoor/config.yml` (TUNNEL_ID filled at install). `cloudflared-frontdoor.service` and `nginx-protecthome.conf` (own accounts, `/home` sealed). `install.sh` (door | tunnel | close; every `sudo` step, logs to `/var/tmp`). `door-rate.sh` (engine fetches and upload per second from nginx's log). `RUNBOOK.md` (nine steps; public last; `cert.pem` deleted after). `scripts/check-public.sh` Phase 1 (eleven assertions; `THROUGH=<n>`). Cloudflare: Cache Rule "Planet: cache the door's replies (W2)" on `.co.uk` — hostname `planet.…`, eligible, Edge TTL from the origin's header (bypass if none), query string ignored; deployed with no DNS record yet (Cloudflare's warning answered "deploy anyway"; the tunnel makes the record). Docs: decisions W2-a…e, secrets map (tunnel key and `cert.pem` rows), Active-Work, Coverage Plan.

**Leave alone (already correct).** Planet: its brief §8 was already amended by Planet session SC-d (WEB D1, Planet commit 463115f); Active-Work's pending item was stale and is cleared. Planet's engine ignores query strings (routes on the path, `server.rs`) and refuses HEAD; the door turns HEAD into GET via the cache — so the plan's "path alone" cache key was right for every address, and the decision round's suggested exception for `?still` was a mistake (`?still` is read by the viewer in the browser).

**Verification.** `npm test`: not run, no change under `site/`. `nginx -t`: clean locally (Ubuntu's nginx 1.28.3 unpacked in WSL, Debian's stock nginx.conf, the door in `conf.d`) — the check of record is PC Claude's on Garcks-PC, pending. Local rehearsal against a stand-in engine with Planet's replies: 100 simultaneous asks → 1 engine fetch; POST and OPTIONS 405; HEAD 200; unknown path 404 without reaching the engine; one visitor's 80 simultaneous asks → 42 answered, 38 × 429, never cached; engine down → 502, `no-store`, no stale copy (rule 10). `bash -n` on every script; `door-rate.sh` and the tunnel ID fill tested on made-up input. Link check and Mojang scan: not run, no links, no room. Secret scan of each staged diff: clean.

**Public check.** `THROUGH=0`: 6/6 PASS (15:48 UTC) — the door is not public yet. Phase 1 against the local door: 10/11, the edge HIT needing Cloudflare, as expected.

**The walk.** None this session — due after PC Claude's run.

**Decisions.** W2-a only our pages may frame the planet — Jamie. W2-b tunnel routing in the repo — Jamie. W2-c system services on their own accounts — Jamie (after a second explanation). W2-d the Cache Rule via Chrome — Jamie. W2-e retire PolicyRAG's quick tunnel — Jamie. Claude's, check-settled: the rate limit (20 r/s, burst 40: a viewer with no edge cache asks ~12/s); nginx keeps the grid one second (not a day) so a resolution change needs one purge at Cloudflare only.

**PC Claude's first run (state commit 6c915e8).** Stopped at step 3 with the machine unchanged: `install.sh` ran `chmod` on its log before `tee` had created it (a race under `set -e`), so it exited at once. Fixed by creating the log with `install -m 0644 /dev/null` before the `tee`; the new preamble passed 50/50 runs in WSL. Also from its report: Planet restarted from year zero at 16:39:57 on the "R20" experiment (same seed, f=32 — the held trigger "a new build or seed": no still yet to refresh, no grid purge needed); PolicyRAG's quick tunnel was started by hand in a terminal (`~/bin/cloudflared tunnel --url http://localhost:8000`), so step 8 is a `pkill` and no PolicyRAG request is needed.

**PC Claude's second run (state commit 53b41ef).** The log fix held; step 3 stopped at `nginx -t`: `mkdir() "/var/cache/nginx/planet" failed` — Ubuntu's package has no `/var/cache/nginx` and nginx makes only the last folder of a cache path. My WSL rehearsal had created that folder by hand, which hid it. Fixed: `install.sh door` makes `/var/cache/nginx/planet` (www-data, 0700) before `nginx -t`; reproduced the failure locally with the folder absent, then clean with the fix; the rehearsal harness no longer pre-creates anything the package doesn't. Left on Garcks-PC until the re-run: nginx 1.24.0 and cloudflared 2026.9.2 installed, the `cloudflared` account made, the configs copied, and nginx still on its stock config (the welcome page on port 80, LAN only). Lesson: a rehearsal must start from what the package installs, not from a prepared folder.

**PC Claude's third run (state commit cf6c98b).** Steps 3–6 passed on c56ea06: the door's eight local checks, the tunnel (ID `a0abe87c-e4cf-4019-adee-05392941e69f`, now committed into `config.yml`) and its service, and the DNS route — **the planet is public at `planet.anjaneyaworkshop.co.uk`** (about 17:30 BST). PC Claude stopped after step 6 because public replies said `max-age=14400`: the zone's Browser Cache TTL (4 h) overrides a shorter origin value unless a rule says otherwise, and I had left the Cache Rule's Browser TTL unset on the wrong belief that unset meant "respect origin". Fixed in the same rule (Browser TTL → Respect origin TTL, the setting on Jamie's approved list); public `Cache-Control` then `max-age=1` on meta, fields and `/`, a day on the grid. Steps 7 (delete `cert.pem`) and 8 (the quick tunnel) not yet run.

**Public check, 16:34 UTC: 17/17 PASS** — Phase 0 6/6; Phase 1 11/11: elevation 35,285 bytes gzipped (41,000 raw), `X-Planet-Tick` 8232, `max-age=1` on meta and fields, a day on the grid, POST 405, HEAD 200, edge HIT on the second fetch (first try), CORS and `frame-ancestors` for the `.co.uk`, unknown path 404. A run a minute earlier (16:33) failed five Phase 0 assertions: for a few minutes this laptop's lookups sent the bare `.co.uk` (and once `www`) to 207.207.210.229 (`Server: openresty`, not Cloudflare; TLS refused) while Windows' resolver, BT's servers, 1.1.1.1 and 8.8.8.8 all answered Cloudflare and a fetch pinned to Cloudflare's address gave 200. It cleared by itself; cause not found. Held as a trigger in Active-Work.

**PC Claude's steps 7–9 (state commit 3a5d51f).** `cert.pem` deleted (`~/.cloudflared` empty); the quick tunnel was already gone (its terminal closed; nothing restarts it); PolicyRAG still answers on :8000 on the LAN.

**The walk, first look (Jamie, laptop).** First "not showing on the site": the planet is at `planet.…`, the hub is still the holding page until Phase 2. Then: "runs smooth for 3–7 seconds, then a jump / catch up / lag". Measured: in a browser, pictures every ~2.0 s (median 2,016 ms) with a gap of 2,983–3,170 ms every few pictures; the viewer glides only across gaps under 3 s (`GLIDE_LONGEST_GAP_MS`, Planet's code) and jumps past that. Three stacked one-second holds — door, edge, and the browser's own copy — make the late ones; without the browser's copy (a node probe, 60 s) the longest gap was 2,389 ms and none passed 3 s. Change (W2-f, Claude's, check-settled): meta and fields now `public, max-age=0, s-maxage=1` — the edge still holds a second, browsers hold nothing, so the house's load is unchanged. Tested locally (headers right, `nginx -t` clean); to be installed by `install.sh door` and re-measured in a browser. Pictures will still come about every 2 s, not every second as WS · D4 expected: door and edge stack; noted for the next decision round.

**The laptop's intermittent TLS failures, explained.** Porkbun's `maceio.ns.porkbun.com` still answers for the domain with its parking addresses (207.207.210.229/.107, `openresty`), and one of BT's resolvers (81.139.57.100) still sometimes asks it (one answer in six); the `.uk` registry and BT's NS records say Cloudflare. Leftover from the morning's nameserver move; it clears as BT's cache expires. Held in Active-Work.

**W2-f installed (PC Claude, state acd296d), re-measured.** In a browser, 40 s after a reload: 19 pictures, gaps median 2,226 ms, max 2,368 ms, **none over 3 s** (before: 3 of 19 over 3 s, max 3,170 ms); steady rather than ragged. Public check 16:54 UTC: Phase 1 11/11 with the new header; Phase 0's two `.com` redirects failed on that run and passed at once when repeated (pinned to Cloudflare's address and unpinned) — the nameserver leftover above.

**The walk, second look (Jamie, after W2-f).** "It runs super smooth. But it doesn't appear to show as much movement when compared with the live planet — the twinkling of the sea, the movement of the plates — it happens much more subtly. Not a major issue for now, but worth commenting." Most likely cause: a picture every ~2.2 s (5–6 ticks) instead of every tick (0.4 s) on the kiosk; changes that come and go between two pictures are never seen, and the rest arrive as one soft blend. The cost of WS · D4's snapshot rate, not a fault. (First guessed as door-and-edge stacking; the gate below corrected that.) Phone on mobile data: works; it opens very zoomed in — the viewer sizes the globe to the screen's height, so a portrait phone sees the middle only. `?zoom=0.42` fits at 390×844; Phase 2's frame will set it; the proper fix requested of a Planet session in the Desktop note, at Jamie's yes (rule 8).

**The gate (PC Claude, state 8244f81, 18:06 BST, three browsers, 60 s).** 174 requests at the door (2.9/s); **28.9 KB/s up the line** (budget under 600 — about 5%); 145 engine fetches (2.42/s), **0.48 per address per second** for meta and the four fields the viewer draws (budget about one). Reading: the edge asks the door only every ~2.1 s per address even at `s-maxage=1`, and most of those go on to the engine — so the ~2.2 s picture is Cloudflare's own refresh, and the door holding nothing would not make pictures come faster. **Phase 1 gate passed: closed.**

**Screens.** `docs/screens/phase-1/`: `laptop-1440x900.png`, `phone-390x844-default.png` (the overflow), `phone-390x844-zoom042.png` (fits) — headless Edge through the public address, `?still` (at 390 px wide it rendered whole this time, unlike W1's holding page).

**Lessons (no new rule; CLAUDE.md is over budget).** A rehearsal must start from what the package installs, not from a prepared folder (the missing `/var/cache/nginx`). An unset Cloudflare setting is not "respect origin": the zone's default wins (the 4 h Browser TTL). Measure the viewer's own rhythm through the public path before calling a door "done" — the walk found what the checks could not.

**Decisions, final.** W2-a…e Jamie's (see above). W2-f Claude's, check-settled: meta and fields `public, max-age=0, s-maxage=1` — before 3 of 19 gaps over 3 s (max 3,170 ms), after 0 of 19 (max 2,368 ms); to confirm or undo at W3's decision round. Planet request (phone zoom) — Jamie, yes.

**Deferred.** A livelier public picture (held: paid plan or push relay, measured first). The phone zoom's proper fix (Planet session). The CLAUDE.md trim (15.6 KB; this session changed one phrase of rule 9). The how-it-works page (after Phase 1 — now due; its own session or the tail of one). The budget covers the fields the page draws, not a visitor cycling all thirteen (Coverage Plan).

### Session W3 — The landing page (September 24, 2026)
Phase 2's first session: the decision round (W2-f and seven more), then the page built, deployed and checked at the public address. Planned split: W3 the page, the line, the fallbacks and the deploy (with a still taken by hand); W4 the still's daily Action, the public check's new assertions, the three-state screens and the gate.

**What changed.**  `site/public/`: `index.html` (the holding page became the landing page: still, framed viewer, name, words, one link), `style.css` (full-screen frame, faint-grey mono line, halo behind the words, words at the very bottom), `_headers` (CSP opened four ways: `script-src 'self'`, `img-src 'self'`, planet for `frame-src` and `connect-src`), `js/main.js` (wiring), `js/layout.js` (frame box and `?zoom=` so the centred globe lands between the name and the words; frame 56 px past every edge hides the viewer's corner buttons), `js/line.js` (the line and "last seen"), `js/watch.js` (live / still after 10 s of failures / "resumed" on a lower tick; the frame shown only after the door has answered since it loaded, 2.5 s after load until Planet's "drawn" message exists), `js/planet.js` (the one address; the frame asks for `?embed`), `still/planet.webp` + `planet.json` (tick 17139, 64 KB). `site/test/`: 51 tests (tick 5, line 7, layout 21, watch 13, page 5; the last added after the first deploy: a tab back from the background starts its ten seconds afresh). `scripts/still-refresh.mjs` (no libraries: drives Edge or Chrome over the DevTools protocol; hides the viewer's corner furniture in its own headless copy; `STILL_RESOLVE` pins the address). Planet request (W3-a, the quiet `?embed`) appended to `Desktop/for-laptop-claude.txt` for Jamie to relay. Docs: decisions W3-a…g, W2-f confirmed; this entry; Active-Work; Coverage Plan.

**Leave alone (already correct).** The front door: `?embed&zoom=` reaches the viewer untouched (the door caches on the path; the viewer reads the query in the browser). The viewer already recovers by itself after an outage (it polls every 125 ms and shows "engine not answering…" in its hidden panel); the hub reloads the frame on recovery anyway, in case it never started. The page asks `/api/meta` with the browser's ordinary caching, never `no-store`, so Cloudflare's one-second copy answers it (checked: edge HIT).

**Verification.** `npm test` 50/50 locally before each of four commits. Dev server (localhost, where the door refuses both framing and CORS, so it shows the offline state for real): the still, "last seen at 18:35" (the still's own time) after 10 s, one console warning naming the cause; phone 375×812 fits. Public address, headless Edge (the in-app browser pane counts as a hidden tab and correctly asks nothing while hidden): laptop 1440×900 — still at 0.7 s, live frame faded in at 3.7 s, line ticking (1.8295 → 1.8311); phone 390×844 — live at 3.5 s, globe the full width less gutters, line on one line. Console: only Chrome's generic note on `sandbox="allow-scripts allow-same-origin"`, harmless for a frame from another address. `nginx -t` not run: no change under `frontdoor/`. Link check: one link, Planet, 200. Mojang scan: not a room.

**Public check.** `check-public.sh` 17:44 UTC: **17/17 PASS** (Phase 0 6/6, Phase 1 11/11; elevation 34,856 bytes gzipped). The hub serves the new CSP exactly as written; the still 65,772 bytes in 0.11 s. Time to first picture: the still at 0.7 s; the live globe at 3.5–3.7 s, of which 2.5 s is the fixed wait for want of Planet's "drawn" signal — the 2 s gate is measured at W4 with `?embed` in.

**The walk.** Jamie, first look (pane preview, laptop): "move the text to the very bottom of the screen — leave Anjaneya where it is." Done: the words sit 12 px from the bottom edge and the globe centres in the band between the name and the words (commit 4308390). Jamie's look at the public address, laptop Chrome: `planet.…` gave `ERR_QUIC_PROTOCOL_ERROR`. Not the site: at the same minute every file of the room and the hub loaded over forced HTTP/3 (headless Edge, `--origin-to-force-quic-on`), all 200 `h3`; the laptop's BT resolvers (81.139.56.100/.57.100) returned Porkbun's parking addresses (207.207.210.107/.229) for `planet.` in 1 lookup of 6 — Chrome tried HTTP/3 to the parking server. The held BT leftover (cached pre-move NS, up to two days: clears ~26 Sep). Chrome's workaround: clear host cache and flush socket pools in `chrome://net-internals`. The phone on mobile data does not use BT's resolvers. Then, at anjaneyaworkshop.co.uk in Chrome: the still top left, the viewer in a small bordered box with its ☰, scrollbars — Chrome was using the holding page's `style.css` (3 rules, from its own cache): the zone's Browser Cache TTL (4 hours, the default W2 met on `planet.`) had turned Pages' `max-age=0` into `max-age=14400` for every file but the page itself. W3-i: the zone set to "Respect Existing Headers" (Jamie's word; made in Jamie's Chrome); all hub files now `public, max-age=0, must-revalidate`; `check-public.sh` Phase 2 asserts it (21/21). A hard reload refreshed Jamie's copy; laptop: works (Jamie). Phone: "not working" — likely the same stale style from Jamie's W1 phone visit, which a phone cannot hard-reload, so the page now asks for `style.css?v=w3` (commit d5c1bc6). The check after it was red once (12 FAIL, all `000`): this laptop's DNS cache held Porkbun's 207.207.210.107 for `planet.` and `www.` (W3-h); pinned to Cloudflare, 200; green 21/21 at 18:13 UTC when the cached answer expired. Phone, after the fix: works (Jamie). **Walk: works on the laptop and the phone.**

**Decisions.** W2-f confirmed (Jamie). W3-a quiet `?embed` requested of Planet, crop meanwhile · W3-b full-screen frame · W3-c "1.4731 billion years · 34% land · highest peak 2,905 m" in a faint grey · W3-d the still after ~10 s of failures · W3-e a daily GitHub Action for the still (rule 16 gains a still-only bot in W4) · W3-f time to first picture measured both ways · W3-g no sentence — all Jamie's. Claude's (not check-settled; for Jamie's walk): below a billion years the age still reads in billions ("0.0421 billion years") so the line keeps one form after a restart from year zero.

**Deferred.** W4: the daily Action, `check-public.sh` Phase 2 (framing allowed, still under a day old, time to first picture), screens of the still and "resumed" states, the gate (network unplugged a minute; `planet.service` restarted at Jamie's word). Planet's `?embed` and portrait zoom (Planet session working, 24 Sep); when `?embed` lands, the hub's 2.5 s wait gives way to the "drawn" message. Noticed: the planet moves ~0.9 billion years an hour, so even an hour-old still is a different world, and the fade from still to live morphs one into another — a question on the Action's cadence for W4's round.

### Session W4 — Phase 2 continued: the look, the still's Action, the public check, and the planet's pauses (September 24, 2026)
Opened with the decision round (W4-a…d); built the look change, the fade through dark, the daily still Action and check-public Phase 2; then, at Jamie's asks, a Cloudflare experiment (W4-e, tried and undone), phone tests, a 5-minute pause monitor, and a Planet request that Planet Claude made live the same evening. Ended at Jamie's word for a fresh session, with the glide's feel the open question.

**What changed.**  `site/`: the Planet link under the name, the line at the very bottom, every word but the name fainter (W4-a); the still and the live globe hand over through the dark (W4-b; `#still.gone`, delayed fades); a phone turned hides the old shape's picture at once and moves the still (`reshapes()` in layout.js); `style.css?v=w4b`; 57 tests (54 → 57). `.github/workflows/still.yml`: the still retaken daily at 11:17 UTC, commits only `site/public/still/` (rule 16 names the bot); run 1 red ("gave up waiting for the browser to start after 20 s"), run 2 green in 2 m 52 s (tick 31,723); `scripts/still-refresh.mjs` now reports the browser's own error output. `scripts/check-public.sh` Phase 2: the hub's framing, the still under 26 h (W4-c), time to first picture by timed fetches — 24/24. `scripts/edge-probe.mjs`, `scripts/hub-monitor.mjs`, `scripts/hub-monitor-report.mjs` (new). `frontdoor/nginx.conf`: W4-e installed then undone (config as W2 plus a comment on why the hold stays); `door-rate.sh` counts BYPASS; RUNBOOK "At the Phase 2 gate". `docs/process/requests/planet-embed.md`: the whole Planet request in one place. CLAUDE.md: rule 16's bot; the three Claudes named as Jamie does. Strategic Plan WS · D4/D13 notes. Screens: `docs/screens/phase-2/w4-*` (live, resumed, a turn, the pinch that clipped).

**Leave alone (already correct).** The door's one-second hold on meta and fields: it is what keeps the viewer's four fields on one tick (W4-e). The page's own recovery after an outage.

**Verification.** `npm test` 57/57. `nginx -t` clean at both installs (Desktop Claude, `frontdoor/state/` c32e385 and 61f43f6). check-public 24/24 after each deploy (time to first picture 0.41–0.53 s; the still 0.0 h old after the Action). Link check: one link, unchanged. Mojang scan: not a room.

**Public check.** Edge probe on elevation_m, 60 s: a new tick every 2,119 ms (median) with the door's hold; 1,068 ms without it (W4-e) — but then the four fields never matched (0 sets in 40 s, 342 replies) and the planet stopped changing. Pause monitor, 5 min before Planet's change: 135 matching pictures, median 2.1 s apart; 7 stalls of 3.1–3.5 s (the door's held copy); one 14 s freeze (the four fields split one tick apart in pairs for six refreshes); no failures, replies ~50 ms, engine ~2.4 ticks/s. Planet's WEB · D5 (`?embed`: no buttons, no tap table, no H/T, "drawn", zoom floor, zoom by message, glide 2.5 behind / 5 s, one-tick splits shown) live at 21:45:05 BST (Planet bbb005a). **After Planet's change** (the same monitor, 11 min from 21:45, three headless browsers on the laptop): 278 matching pictures, still median 2.1 s apart; 22 gaps of 3–4 s (the new glide should now bridge these — not visible from outside); long freezes of 10, 11, 13 and **38 s**: elevation stayed 2–4 ticks out of step with the three river fields (beyond item 9's one-tick allowance). Likely mechanism: after a split the viewer re-asks only the fields behind, which puts them on another refresh phase at Cloudflare's shared copy, and the split persists; viewers sharing a Cloudflare location may make it worse (a guess). No failures; field replies median 59 ms. The restart showed "resumed" 2 s after 21:45:05, laptop and phone, no reload.

**The walk.** Laptop and phone: the new look accepted. Phone: pinch out clipped the globe and would not zoom out (Planet's 0.4 floor below the hub's 0.373 — Planet item 5, now live, not yet re-walked); turning was odd (fixed on the hub); swipe-to-refresh does not work (the frame takes every swipe — open). "A still image spinning" after W4-e (undone). "Every now and again it pauses then continues" → the monitor above. After Planet's change: **"too drawn out — too slow and still not keeping up with itself"** — the first item for W5.

**Decisions.** W4-a the look · W4-b daily still, handover through the dark · W4-c red after 26 h · W4-d hold Phase 2 open on the 2 s line until `?embed` · W4-e the door stops holding the live numbers — all Jamie's; W4-e undone the same evening on the measured freeze. Planet request to Planet Claude (Jamie): items 8, 9, 1–6 tonight; item 7 (fields as one) later.

**Deferred.** The glide's feel (W5 first). The hub's side of `?embed`: drop the 56 px crop; send the zoom on a turn instead of reloading. Re-time the live globe with "drawn" (first try under load: 2.9–4.5 s — not yet a clean measure). Gate: the network unplugged a minute, `door-rate.sh 60`, Jamie's walk (the restart half passed: "resumed" 2 s after the 21:45:05 restart, laptop and phone, no reload). Swipe-to-refresh (leave, or try the top strip). Planet item 7. Trim CLAUDE.md (now 16.2 KB).

### Session W5-a — Planet item 7 set up (September 24, 2026)
Jamie asked whether to jump straight to Planet request item 7 (the fields as one) rather than retune the glide first, and how much work it is. Yes: the glide is being tuned against freezes item 7 removes. Jamie chose one request per picture (W5-a). This session wrote the request and the door's side and handed over; nothing is live yet.

**What changed.** `docs/process/requests/planet-embed.md`: item 7 moved from "for later" to now, with the contract (`GET /api/picture/<field>`, one tick, `X-Planet-Tick`, compressible type, 404 for an unknown field, `/api/field/` unchanged, the viewer warns and falls back to four requests if the door refuses). `frontdoor/nginx.conf`: the allow-list lets `api/picture/[a-z0-9_]+` through (not installed yet). `frontdoor/RUNBOOK.md`: §For Planet item 7. Handover to Desktop Claude (the door first) and Planet Claude (item 7) at the end of `Desktop/for-laptop-claude.txt`.

**Leave alone (already correct).** The door's one-second hold, the cache map (the new address falls under "one second at the edge"), the hub.

**Verification.** `npm test`: not run, no change to `site/`. `nginx -t`: pending, Desktop Claude at install (runbook §For Planet item 7). `check-public.sh`: not run, the door change is not installed; runs after install and again after Planet's build. Link check: not run, no link changed.

**Public check.** No change.

**The walk.** None this session.

**Decisions.** W5-a — item 7 now, as one request per picture (Jamie).

**Deferred.** After Planet's build: the pause monitor re-run; `check-public.sh` gains the picture address; the door's hold tried off again (W4-e), measured; the glide retuned to what is left; the rest of W5 (the hub's side of `?embed`, the gate).

**W5-a, continued — item 7 live, and the door's hold (W5-b).** Door ready 22:19 (Desktop Claude, 9a2d8ac); Planet's build live 22:29 BST (restart 51,093 → 51,009; the hub said "resumed" by itself, no reload). `/api/picture/elevation_m`: 200, gzip, `X-Planet-Tick`, `s-maxage=1`, edge HIT; 164,016 bytes (the four fields' 163,872 plus a 144-byte header), 59.0 KB on the wire against 58.9 KB for the four separately; unknown field 404, POST 405. Probe, 90 s at 4 asks a second: a new picture every 2,066 ms (median), p90 2,318, max 2,552 — no gap over 3 s (before: freezes of 10–38 s). Jamie: "better but I'm still getting lags" — the pace, not freezes. W5-b (Jamie): the door stops holding the picture; `frontdoor/nginx.conf` (a bypass map, as W4-e), runbook §For Planet item 7 W5-b; not installed at this commit. `scripts/hub-monitor-report.mjs` counts one-request pictures. Next: install, probe again, then Planet shortens the glide to the new pace.

**W5-b installed 22:37:43 BST (Desktop Claude, 74862d7), and W5-c.** Probe after, 90 s: a new picture every 1,043 ms (median), p90 1,273, max 1,289 — every gap under 1.5 s (before: 2,066). Hub monitor, 10 min from a fresh load (22:31–22:41, spanning the install): 293 picture asks, 0 single fields; gaps median 2,131 ms with 19 over 3 s (max 5.3 s), almost all before the install; after it, 97 asks and 97 new ticks in 196 s — one ask every ~2.0 s. The viewer asks when `/api/meta` shows a new tick, and meta (still held) changed every 2,066 ms (40 s probe). W5-c (Claude, by the numbers): meta not held at the door either; `nginx.conf`, runbook W5-c; not installed at this commit. Jamie: "still stuttering" — the viewer's ~2 s asking and the long glide (2.5 behind, 5 s), not the supply.

**W5-c installed 22:45:13 BST (Desktop Claude, 99feb51).** Meta probe, 40 s: a new tick every 1,034 ms (median), max 1,268. Hub monitor, 3 min from a fresh load: 188 picture asks, 165 new pictures, gaps median 1,061 ms, p90 1,111, max 2,167, none over 3 s (W4: median 2.1 s, freezes to 38 s); field latency median 74 ms; hub "shown" all 178 s. `check-public.sh`: all phases 0–2 passed. Jamie: "still stutter, but it's close" — the supply is now steady at ~1 s; what is left is inside the viewer (Planet's).
Asked which stutter: **the spin catches about once a second** (Jamie) — the viewer's work when a picture lands, not the supply. The browser pane (throttled ~25 fps, not a fair test) showed no task over 50 ms in 12 s. Handed to Planet Claude as request item 10 (`requests/planet-embed.md`), with the glide's retune second.

**Item 10 live 23:11:30 BST** (Planet f2d6a98, Planet WEB · D7): each picture prepared a slice at a time (≤ 4 ms a frame) into its own GPU buffers, so a picture landing changes only which buffers are drawn; the embedded glide back to 1.5 pictures behind (5 s longest gap kept). Planet Claude on Garcks-PC's GPU: 0 frames over 25 ms in 25 s (was 3). Hub monitor, 3 min from a fresh load 20 s after the restart: 196 picture asks, 153 new pictures, gaps median 1,051 ms, p90 1,922, max 2,586, none over 3 s; no failures; one slow picture (1.4 s) and meta (1.0 s) while the engine replayed after its resume; hub "shown" throughout. The spin itself is Jamie's eye (the monitor draws in software).

**The walk (after item 10, laptop browser).** Jamie: "the spin is the best it's been on browser by far — not perfect, but that's good enough for now." Limitation carried (rule 15): a small catch remains on the laptop, unmeasured (the Chrome frame-rate run did not happen); the phone not yet walked since item 10.

**W5-d — the crop dropped (25 Sep, after midnight).** Jamie: the spin works better on `planet.` than on the hub. The hub ran its frame 56 px past every edge (W3's stopgap for the viewer's buttons, which `?embed` hides since D5): 25% more pixels a frame on the laptop (8.7 M device px against 7.0 M at 1536×729, dpr 2.5), 46% on an upright phone. `site/public/js/layout.js` CROP 56 → 0; the layout test now checks the frame covers the screen and runs no wider. Laptop frame 1536×734, zoom 0.775; phone 390×848, zoom 0.422. The viewer's embed mode hides both buttons and starts its panels hidden, so no edge shows. Glide now alike on both (2.2 behind, Planet WEB · D8); only the longest glided gap differs (5 s embedded, 3 s not). `npm test` 57/57.
Deployed 00:15:50 (Pages); `check-public.sh` all phases 0–2 passed, time to first picture 0.32 s. The walk: Jamie to compare hub and `planet.` on the laptop.

### Session W5-e — The laptop runs hot on the hub (September 25, 2026)
Jamie, before bed: the laptop runs hot with the hub open in Chrome. Measured what does it, found the cause in Planet's viewer (not the hub), and handed the fix to Planet Claude at Jamie's word.

**What changed.** `requests/planet-embed.md` §Item 11; the handover at the end of `Desktop/for-laptop-claude.txt`; decision W5-e. No code.

**Leave alone (already correct).** The hub's own work: one `/api/meta` ask every 2 s and a 250 ms text check — Chrome's processor share 1–2%.

**Verification.** `npm test`, `nginx -t`, `check-public.sh`, link check: not run, no change to `site/` or `frontdoor/`.

**Public check.** No change. Laptop baseline for item 11 (hub open, 10 s, ~00:40): NVIDIA 58–59 °C, 7–11 W, 23–40% busy; Intel 3D Chrome ~13%, dwm ~21%; processor ~30% overall. The viewer's canvas 3072×1728 (drawRatio 2 on a 250% 4K panel).

**The walk.** None this session.

**Decisions.** W5-e — stay on the GPU, save only what the eye cannot see (Jamie).

**Deferred.** Item 11 live at Jamie's word; then the same 10 s measurement on the laptop, before and after.
